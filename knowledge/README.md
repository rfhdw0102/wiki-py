# Knowledge repository

This directory becomes an independent Git repository when the API starts.

- `wiki/` contains published, human-readable compiled knowledge.
- `schema/` contains versioned compilation and publication rules.

PostgreSQL search tables are derived projections and can be rebuilt from this directory.

